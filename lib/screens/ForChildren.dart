import 'package:flutter/material.dart';

class ForChildrenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("ForChildren Page"),
      ),
    );
  }
}
