import 'package:flutter/material.dart';

class ImportantPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Important Page"),
      ),
    );
  }
}
